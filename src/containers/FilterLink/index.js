// Importez la méthode `connect`, l'action `setVisibilityFilter` et le composant `Link`.

// Codez ici la fonction `mapStateToProps`.

// Codez ici la fonction `mapDispatchToProps`.

// Exportez le `container` via la méthode `connect`.
